## Deploy

From within the flake devshell (`direnv allow` or `nix develop`):

```sh
cd mycloud-nixos-2
./deploy.sh
```

This runs `nix run .#deploy-rs -- .#mycloud-nixos-2`.

If you run into cache errors, add `--option eval-cache false`.
